<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/xv_bg">

    <!-- Header: back + title -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="12dp">

        <Button
            android:id="@+id/xv_settings_back"
            android:layout_width="wrap_content"
            android:layout_height="36dp"
            android:text="◀"
            android:textColor="@color/xv_text"
            android:textSize="14sp"
            android:background="@android:color/transparent"
            android:minWidth="40dp"/>

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Settings"
            android:textColor="@color/xv_text"
            android:textSize="20sp"
            android:textStyle="bold"
            android:layout_marginStart="4dp"/>
    </LinearLayout>

    <!-- Tab strip: Devices | TX/RX | Server.
         Devices sits leftmost + is the default because the AINA
         speakermic / external-button / audio-device pickers are the
         controls operators touch every session; TX/RX (hot-mic,
         timeouts, tone) is tuning; Server (TAK server picker, Mumble
         connect/disconnect) is one-time enrollment. Tab IDs were
         historically xv_tab_prefs / xv_tab_txrx / xv_tab_calls —
         those IDs are preserved so the wireSettingsTabs findViewById
         calls in XvDropDownReceiver don't break; only display order,
         labels, and the default-selection index changed. -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:paddingStart="12dp"
        android:paddingEnd="12dp">

        <Button
            android:id="@+id/xv_tab_prefs"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:text="Devices"
            android:textColor="@color/xv_text"
            android:textSize="13sp"
            android:textAllCaps="false"
            android:background="@drawable/xv_button_bg"/>

        <Button
            android:id="@+id/xv_tab_txrx"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:layout_marginStart="6dp"
            android:text="TX/RX"
            android:textColor="@color/xv_text"
            android:textSize="13sp"
            android:textAllCaps="false"
            android:background="@drawable/xv_button_bg"/>

        <Button
            android:id="@+id/xv_tab_calls"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:layout_marginStart="6dp"
            android:text="Server"
            android:textColor="@color/xv_text"
            android:textSize="13sp"
            android:textAllCaps="false"
            android:background="@drawable/xv_button_bg"/>
    </LinearLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="12dp">

            <!-- ============================================================ -->
            <!-- Section 1: TX/RX  (latched mode + TX timeouts)              -->
            <!-- ============================================================ -->
            <LinearLayout
                android:id="@+id/xv_section_txrx"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:visibility="gone">

                <!-- Latched-mode toggle hidden from Settings UI per user
                     direction (2026-05-10). Latched mode remains
                     functional under the hood — direct calls default to
                     latched, and the Switch ID is preserved (visibility
                     gone) so wireTxRxSection's findViewById doesn't NPE.
                     Re-expose by switching visibility back to "visible"
                     when a non-call latched use case comes up. -->
                <Switch
                    android:id="@+id/xv_switch_latched"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:visibility="gone"/>

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Hot mic (PTT latency)"
                    android:textColor="@color/xv_text_dim"
                    android:textSize="11sp"
                    android:textAllCaps="true"
                    android:letterSpacing="0.1"
                    android:layout_marginTop="20dp"/>

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical"
                    android:padding="12dp"
                    android:background="@drawable/xv_card_bg"
                    android:layout_marginTop="4dp">

                    <TextView
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:text="Hot mic mode (instant PTT)"
                        android:textColor="@color/xv_text"
                        android:textSize="14sp"/>

                    <Switch
                        android:id="@+id/xv_switch_hot_mic"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"/>
                </LinearLayout>

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Pre-warms the Bluetooth SCO link for the entire time you're in a channel so the first PTT after a long pause skips the 0.5-1.5s cold-start. Tradeoff: media apps (Spotify, podcasts) stay paused for the whole session, not just during PTT bursts."
                    android:textColor="@color/xv_text_dim"
                    android:textSize="11sp"
                    android:layout_marginTop="6dp"/>

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="PTT timeout (seconds)"
                    android:textColor="@color/xv_text_dim"
                    android:textSize="11sp"
                    android:textAllCaps="true"
                    android:letterSpacing="0.1"
                    android:layout_marginTop="20dp"/>

                <TextView
                    android:id="@+id/xv_label_ptt_timeout"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="30 s"
                    android:textColor="@color/xv_text"
                    android:textSize="14sp"
                    android:fontFamily="monospace"
                    android:layout_marginTop="4dp"/>

                <SeekBar
                    android:id="@+id/xv_slider_ptt_timeout"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:min="20"
                    android:max="90"
                    android:progress="30"
                    android:layout_marginTop="4dp"/>

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Auto-release a stuck PTT after this many seconds (20–90 s)."
                    android:textColor="@color/xv_text_dim"
                    android:textSize="11sp"
                    android:layout_marginTop="4dp"/>

                <!-- Latched timeout slider — hidden alongside the latched
                     toggle (2026-05-10). The SeekBar id is preserved so
                     XvDropDownReceiver.wireTxRxSection's findViewById
                     keeps working; only visibility is gone. -->
                <SeekBar
                    android:id="@+id/xv_slider_latched_timeout"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:max="600"
                    android:progress="180"
                    android:visibility="gone"/>

                <TextView
                    android:id="@+id/xv_label_latched_timeout"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="180 s"
                    android:visibility="gone"/>

                <!-- Talk permit tone lives on the TX/RX tab because
                     it's a transmit behavior, not a device choice —
                     the tone plays when the operator keys, regardless
                     of which speakermic / route is active. Moved here
                     from the Devices tab so the Devices tab is closer
                     to what operators reach for every session. -->
                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Talk permit tone"
                    android:textColor="@color/xv_text_dim"
                    android:textSize="11sp"
                    android:textAllCaps="true"
                    android:letterSpacing="0.1"
                    android:layout_marginTop="20dp"/>

                <Spinner
                    android:id="@+id/xv_spinner_tpt"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:background="@drawable/xv_card_bg"
                    android:popupBackground="@color/xv_surface_alt"/>

                <Button
                    android:id="@+id/xv_btn_test_tpt"
                    android:layout_width="match_parent"
                    android:layout_height="44dp"
                    android:layout_marginTop="8dp"
                    android:text="Test tone"
                    android:textColor="@color/xv_text"
                    android:background="@drawable/xv_button_bg"/>

                <View
                    android:layout_width="match_parent"
                    android:layout_height="40dp"/>
            </LinearLayout>

            <!-- ============================================================ -->
            <!-- Section 2: Server (TAK server + Mumble connect/disconnect).  -->
            <!-- VS1/VS2 channel selection lives on the main view's PTT       -->
            <!-- cards — operators pick channels there, not buried in         -->
            <!-- settings. The legacy VS1/VS2 spinners were deleted per       -->
            <!-- operator feedback that channel choice belongs front-of-     -->
            <!-- screen, not behind a Settings tab.                           -->
            <!-- ============================================================ -->
            <LinearLayout
                android:id="@+id/xv_section_calls"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:visibility="gone">

                <!-- TAK server. Tappable: opens AlertDialog server picker
                     (auto / per-host) so operators with >1 enrolled TAK
                     server can pin which one XV uses for Mumble. Pick
                     persists in SharedPreferences (PREF_TAK_SERVER_HOST)
                     and is honored by connectMumbleWithDefaults on every
                     plugin start. Click handler is bound in
                     XvDropDownReceiver.wireSettings. -->
                <!-- @noinspection KeyboardInaccessibleWidget -->
                <!-- TextView is clickable at runtime; Studio lint nag
                     about contentDescription is intentional — the label
                     text already conveys the affordance. -->

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="TAK server"
                    android:textColor="@color/xv_text_dim"
                    android:textSize="11sp"
                    android:textAllCaps="true"
                    android:letterSpacing="0.1"/>

                <TextView
                    android:id="@+id/xv_tak_server_label"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:padding="12dp"
                    android:background="@drawable/xv_card_bg"
                    android:layout_marginTop="4dp"
                    android:text="—"
                    android:textColor="@color/xv_text"
                    android:fontFamily="monospace"
                    android:textSize="13sp"/>

                <!-- Mumble actions -->
                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Mumble"
                    android:textColor="@color/xv_text_dim"
                    android:textSize="11sp"
                    android:textAllCaps="true"
                    android:letterSpacing="0.1"
                    android:layout_marginTop="20dp"/>

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:layout_marginTop="4dp">

                    <Button
                        android:id="@+id/xv_btn_mumble_connect"
                        android:layout_width="0dp"
                        android:layout_height="44dp"
                        android:layout_weight="1"
                        android:text="Connect"
                        android:textColor="@color/xv_text"
                        android:background="@drawable/xv_button_bg"/>

                    <Button
                        android:id="@+id/xv_btn_mumble_disconnect"
                        android:layout_width="0dp"
                        android:layout_height="44dp"
                        android:layout_weight="1"
                        android:layout_marginStart="8dp"
                        android:text="Disconnect"
                        android:textColor="@color/xv_text"
                        android:background="@drawable/xv_button_bg"/>
                </LinearLayout>

                <View
                    android:layout_width="match_parent"
                    android:layout_height="40dp"/>
            </LinearLayout>

            <!-- ============================================================ -->
            <!-- Section 3: Preferences (TPT, audio output, AINA picker)     -->
            <!-- ============================================================ -->
            <LinearLayout
                android:id="@+id/xv_section_prefs"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:visibility="visible">

                <!-- Persistent banner shown ONLY when the Bluetooth
                     adapter is off. Tapping opens the system BT
                     settings so the operator can turn it back on
                     without leaving XV. Visibility is driven by an
                     ACTION_STATE_CHANGED receiver registered while
                     the dropdown view is attached — see
                     XvDropDownReceiver.wireBtOffBanner. Default is
                     gone so it doesn't flash at inflate; the receiver
                     sets the correct state on the initial poll. -->
                <TextView
                    android:id="@+id/xv_prefs_bt_off_banner"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:padding="12dp"
                    android:layout_marginBottom="16dp"
                    android:background="@drawable/xv_card_bg"
                    android:text="Bluetooth is off — tap to enable"
                    android:textColor="@color/xv_text"
                    android:textSize="13sp"
                    android:clickable="true"
                    android:focusable="true"
                    android:visibility="gone"/>

                <!-- Section header: PTT input -->
                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="PTT INPUT"
                    android:textColor="@color/xv_text_dim"
                    android:textSize="11sp"
                    android:letterSpacing="0.15"/>

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Where the PTT button lives. Picked separately from audio so an AINA speakermic + car Bluetooth (or headphones) is one operator-tap apart."
                    android:textColor="@color/xv_text_dim"
                    android:textSize="11sp"
                    android:layout_marginTop="2dp"/>

                <!-- AINA picker — moved up to be visually paired with the
                     "PTT INPUT" header. Wiring is unchanged; this is the
                     same xv_spinner_aina the AINA picker code targets. -->
                <Spinner
                    android:id="@+id/xv_spinner_aina"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:background="@drawable/xv_card_bg"
                    android:popupBackground="@color/xv_surface_alt"/>

                <TextView
                    android:id="@+id/xv_label_aina_status"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="—"
                    android:textColor="@color/xv_text_dim"
                    android:textSize="11sp"
                    android:fontFamily="monospace"
                    android:layout_marginTop="4dp"/>

                <!-- Section header: External button input -->
                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="EXTERNAL BUTTON"
                    android:textColor="@color/xv_text_dim"
                    android:textSize="11sp"
                    android:letterSpacing="0.15"
                    android:layout_marginTop="24dp"/>

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Optional BLE PTT puck (e.g. Pryme BT-PTT). Provides an extra push-to-talk button; must be button-only — speakermics belong in the Primary slot. Either button can key the primary channel without one cutting off the other."
                    android:textColor="@color/xv_text_dim"
                    android:textSize="11sp"
                    android:layout_marginTop="2dp"/>

                <Spinner
                    android:id="@+id/xv_spinner_aina_secondary"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:background="@drawable/xv_card_bg"
                    android:popupBackground="@color/xv_surface_alt"/>

                <TextView
                    android:id="@+id/xv_label_aina_secondary_status"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="—"
                    android:textColor="@color/xv_text_dim"
                    android:textSize="11sp"
                    android:fontFamily="monospace"
                    android:layout_marginTop="4dp"/>

                <!-- Scan for BLE PTT buttons that don't pair via the
                     phone's system Bluetooth settings (they advertise a
                     vendor service, not HID / HoGP). Opens a dialog that
                     runs a BluetoothLeScanner filtered for the HM-10
                     transparent-UART service UUID (Pryme BT-PTT-Z,
                     PTT-Z01, similar HM-10 buttons) plus a name filter
                     for anything advertising "PTT". On tap, operator
                     picks the device + which slot (primary or external
                     button) to assign it to; XV connects via unbonded
                     GATT through PrymeBleReader. -->
                <Button
                    android:id="@+id/xv_btn_scan_ble_ptt"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="12dp"
                    android:text="Scan for BLE PTT device"
                    android:textColor="@color/xv_text"
                    android:textAllCaps="false"
                    android:background="@drawable/xv_button_bg"
                    android:minHeight="44dp"/>

                <!-- Remove a previously-added BLE PTT device. Shows a
                     dialog listing every MAC the operator has added via
                     the scan-and-pick flow above, then removes it from
                     the picker + any slot assignment. -->
                <Button
                    android:id="@+id/xv_btn_remove_ble_ptt"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:text="Remove BLE PTT device"
                    android:textColor="@color/xv_text"
                    android:textAllCaps="false"
                    android:background="@drawable/xv_button_bg"
                    android:minHeight="44dp"/>

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="For BLE PTT buttons that won't pair via system Bluetooth (they advertise a vendor service, not HID)."
                    android:textColor="@color/xv_text_dim"
                    android:textSize="11sp"
                    android:layout_marginTop="4dp"/>

                <!-- Auto-connect toggle for compatible BT speakermics / buttons -->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical"
                    android:padding="12dp"
                    android:background="@drawable/xv_card_bg"
                    android:layout_marginTop="16dp">

                    <TextView
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:text="Auto-connect compatible Bluetooth"
                        android:textColor="@color/xv_text"
                        android:textSize="14sp"/>

                    <Switch
                        android:id="@+id/xv_switch_auto_connect_bt"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"/>
                </LinearLayout>

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="When ON, XV automatically connects the first compatible bonded AINA / BLE PTT button it finds on launch. Turn OFF to control connections manually from the pickers above. Persists across launches."
                    android:textColor="@color/xv_text_dim"
                    android:textSize="11sp"
                    android:layout_marginTop="4dp"/>

                <!-- Samsung ruggedized-device Active Key PTT toggle.
                     Visible ONLY on Samsung Tab Active5 / XCover6 Pro
                     / XCover7 / Tab Active4 Pro / Tab Active3 — the
                     row is hidden (`View.GONE`) at inflate time on
                     every other device via
                     XvDropDownReceiver.wireSamsungActiveKeySwitch,
                     which consults
                     `SamsungActiveKey.isSupported(context)`. Non-
                     Samsung operators never see this row. Default
                     OFF; the operator opts in explicitly. -->
                <LinearLayout
                    android:id="@+id/xv_row_samsung_active_key"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical"
                    android:padding="12dp"
                    android:background="@drawable/xv_card_bg"
                    android:layout_marginTop="12dp"
                    android:visibility="gone">

                    <TextView
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:text="Use Samsung Active Key as PTT"
                        android:textColor="@color/xv_text"
                        android:textSize="14sp"/>

                    <Switch
                        android:id="@+id/xv_switch_samsung_active_key"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"/>
                </LinearLayout>

                <TextView
                    android:id="@+id/xv_label_samsung_active_key_help"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Use the programmable side button on your Samsung ruggedized device as an additional PTT source, alongside on-screen PTT and any bonded AINA / BLE PTT button. Press to talk; release to end. Turn OFF to restore the key's default Samsung behaviour."
                    android:textColor="@color/xv_text_dim"
                    android:textSize="11sp"
                    android:layout_marginTop="4dp"
                    android:visibility="gone"/>

                <!-- Background Active Key PTT via Accessibility service.
                     Visible ONLY on Samsung ruggedized hardware (same
                     isSupported() gate as the row above).  When enabled,
                     the Samsung Active Key fires PTT even while ATAK is
                     backgrounded.  Requires the operator to grant XV the
                     Accessibility permission via the system settings
                     dialog — cannot be enabled programmatically.
                     The button deep-links to the system Accessibility
                     settings page; the toggle reflects whether the
                     service is currently enabled (read-only — toggling
                     navigates to OS settings rather than toggling in-
                     place, because the OS permission cannot be granted
                     or revoked by the app). -->
                <LinearLayout
                    android:id="@+id/xv_row_samsung_active_key_bg"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical"
                    android:padding="12dp"
                    android:background="@drawable/xv_card_bg"
                    android:layout_marginTop="12dp"
                    android:visibility="gone">

                    <TextView
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:text="Use Active Key while ATAK is in the background"
                        android:textColor="@color/xv_text"
                        android:textSize="14sp"/>

                    <Switch
                        android:id="@+id/xv_switch_samsung_active_key_bg"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"/>
                </LinearLayout>

                <TextView
                    android:id="@+id/xv_label_samsung_active_key_bg_help"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Requires granting XV the Accessibility permission so the Active Key fires PTT while ATAK is in the background. Does NOT read screen content, typed text, or any other app data — only the Active Key button is intercepted. Tap the toggle to open Android Accessibility Settings to enable or disable the XV service."
                    android:textColor="@color/xv_text_dim"
                    android:textSize="11sp"
                    android:layout_marginTop="4dp"
                    android:visibility="gone"/>

                <!-- Sonim ruggedized-device dedicated PTT side button
                     toggle. Visible ONLY on Sonim XP10 / XP9900 and
                     XP-family peers — the row is hidden (`View.GONE`)
                     at inflate time on every other device via
                     XvDropDownReceiver.wireSonimPttButtonSwitch, which
                     consults `SonimHardwareButtons.isSupported(context)`.
                     Non-Sonim operators never see this row. Default OFF;
                     the operator opts in explicitly. -->
                <LinearLayout
                    android:id="@+id/xv_row_sonim_ptt_button"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical"
                    android:padding="12dp"
                    android:background="@drawable/xv_card_bg"
                    android:layout_marginTop="12dp"
                    android:visibility="gone">

                    <TextView
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:text="Use Sonim PTT button as PTT"
                        android:textColor="@color/xv_text"
                        android:textSize="14sp"/>

                    <Switch
                        android:id="@+id/xv_switch_sonim_ptt_button"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"/>
                </LinearLayout>

                <TextView
                    android:id="@+id/xv_label_sonim_ptt_button_help"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Use the dedicated PTT side button on your Sonim ruggedized device as an additional PTT source, alongside on-screen PTT and any bonded AINA / BLE PTT button. Press to talk; release to end. Turn OFF to restore the button's default Sonim behaviour."
                    android:textColor="@color/xv_text_dim"
                    android:textSize="11sp"
                    android:layout_marginTop="4dp"
                    android:visibility="gone"/>

                <!-- Sonim ruggedized-device dedicated Emergency / SOS
                     button toggle. Same gate as the PTT-button row
                     above. Currently a plain additional PTT source; a
                     future PR may promote it to fire an emergency CoT
                     event or SOS broadcast without changing this row. -->
                <LinearLayout
                    android:id="@+id/xv_row_sonim_emergency_button"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:gravity="center_vertical"
                    android:padding="12dp"
                    android:background="@drawable/xv_card_bg"
                    android:layout_marginTop="12dp"
                    android:visibility="gone">

                    <TextView
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_weight="1"
                        android:text="Use Sonim Emergency button as PTT"
                        android:textColor="@color/xv_text"
                        android:textSize="14sp"/>

                    <Switch
                        android:id="@+id/xv_switch_sonim_emergency_button"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"/>
                </LinearLayout>

                <TextView
                    android:id="@+id/xv_label_sonim_emergency_button_help"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Use the dedicated red Emergency / SOS button on your Sonim ruggedized device as an additional PTT source. For now, press to talk; release to end. A future update may add an emergency CoT event or SOS broadcast."
                    android:textColor="@color/xv_text_dim"
                    android:textSize="11sp"
                    android:layout_marginTop="4dp"
                    android:visibility="gone"/>

                <!-- Section header: Audio device -->
                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="AUDIO DEVICE"
                    android:textColor="@color/xv_text_dim"
                    android:textSize="11sp"
                    android:letterSpacing="0.15"
                    android:layout_marginTop="24dp"/>

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Where you hear (and speak) — speaker/mic pair. Auto follows the PTT input device when it has audio (e.g. AINA speakermic), otherwise falls back to the built-in route below. Pick a specific Bluetooth device to split: PTT from AINA, audio through car/headphones."
                    android:textColor="@color/xv_text_dim"
                    android:textSize="11sp"
                    android:layout_marginTop="2dp"/>

                <Spinner
                    android:id="@+id/xv_spinner_bt_audio_override"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="8dp"
                    android:background="@drawable/xv_card_bg"
                    android:popupBackground="@color/xv_surface_alt"/>

                <!-- Built-in fallback route (only used when Auto + no
                     Bluetooth audio device is present) -->
                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Built-in fallback (no BT)"
                    android:textColor="@color/xv_text_dim"
                    android:textSize="11sp"
                    android:textAllCaps="true"
                    android:letterSpacing="0.1"
                    android:layout_marginTop="16dp"/>

                <Spinner
                    android:id="@+id/xv_spinner_route"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:background="@drawable/xv_card_bg"
                    android:popupBackground="@color/xv_surface_alt"/>

                <TextView
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:text="Used when Audio Device above is Auto AND no Bluetooth or wired headset is connected. Choose phone speaker for hands-free, earpiece for hold-to-ear."
                    android:textColor="@color/xv_text_dim"
                    android:textSize="11sp"
                    android:layout_marginTop="4dp"/>

                <View
                    android:layout_width="match_parent"
                    android:layout_height="40dp"/>
            </LinearLayout>
        </FrameLayout>
    </ScrollView>
</LinearLayout>
